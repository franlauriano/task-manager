name: Retrieve Team API Test - Bad Request (400)
version: "1.0"
testcases:
  - name: Retrieve team - Invalid UUID format
    steps:
      - type: http
        method: GET
        url: "{{.base_url}}/api/teams/invalid-uuid-format"
        headers:
          Accept: "application/json"
        assertions:
          - result.statuscode ShouldEqual 400
          - result.bodyjson ShouldNotBeNil
